

# Experiment metadata
eval_name: linear_probe_eval
tag: echofm-base-4class-probe-linear  # Experiment tag for logging
folder: ./experiments/linear_probe/              # Output folder for checkpoints and logs
num_workers: 8
resume_checkpoint: false

# Experiment configuration
experiment:
  classifier:
    probe_type: linear
    use_layernorm: true
    dropout: 0.0

  data:
    # Dataset configuration
    dataset_type: VideoDataset

    # Paths to training and validation datasets
    # Supports both CSV files and folder paths
    # CSV format: path/to/video.mp4,label[,patient_id]
    # If patient_id is present in the 3rd column, subject-level validation accuracy is computed.
    dataset_train: /system/user/publicwork/fallmann/miracle/miracle/data/server/train_val_4class.csv
    dataset_val:  /system/user/publicwork/fallmann/miracle/miracle/data/server/test_4class.csv

    # Number of classification classes
    num_classes: 4

    # Video sampling parameters
    resolution: 224              # Input image resolution
    frames_per_clip: 32          # Frames per video clip
    frame_step: 1                # Step between consecutive frames
    num_segments: 1              # Number of temporal segments
    num_views_per_segment: 1     # Spatial views per segment

  optimization:
    batch_size: 20
    num_epochs: 12
    use_bfloat16: false          # Use mixed precision training

    # Hyperparameter grid: train multiple probes with different settings
    # Useful for finding optimal hyperparameters
    multihead_kwargs:
      # --- Group 1: 1e-2 (Higher LR for linear) ---
      - final_lr: 0.0
        final_weight_decay: 0.0
        lr: 0.01
        start_lr: 0.01
        warmup: 0.0
        weight_decay: 0.0
      - final_lr: 0.0
        final_weight_decay: 0.0001
        lr: 0.01
        start_lr: 0.01
        warmup: 0.0
        weight_decay: 0.0001
      - final_lr: 0.0
        final_weight_decay: 0.01
        lr: 0.01
        start_lr: 0.01
        warmup: 0.0
        weight_decay: 0.01

      # --- Group 2: 5e-3 ---
      - final_lr: 0.0
        final_weight_decay: 0.0
        lr: 0.005
        start_lr: 0.005
        warmup: 0.0
        weight_decay: 0.0
      - final_lr: 0.0
        final_weight_decay: 0.0001
        lr: 0.005
        start_lr: 0.005
        warmup: 0.0
        weight_decay: 0.0001
      - final_lr: 0.0
        final_weight_decay: 0.01
        lr: 0.005
        start_lr: 0.005
        warmup: 0.0
        weight_decay: 0.01

      # --- Group 3: 1e-3 (Overlap with your old grid) ---
      - final_lr: 0.0
        final_weight_decay: 0.0
        lr: 0.001
        start_lr: 0.001
        warmup: 0.0
        weight_decay: 0.0
      - final_lr: 0.0
        final_weight_decay: 0.0001
        lr: 0.001
        start_lr: 0.001
        warmup: 0.0
        weight_decay: 0.0001
      - final_lr: 0.0
        final_weight_decay: 0.01
        lr: 0.001
        start_lr: 0.001
        warmup: 0.0
        weight_decay: 0.01

# Model configuration
model_kwargs:
  # Pretrained EchoFM checkpoint path
  # Leave empty to use random initialization
  checkpoint: /system/user/publicwork/fallmann/miracle/echofm/checkpoints/echofm.pt

  # Model name (supported: mae_vit_base_patch16, mae_vit_large_patch16)
  model_name: mae_vit_large_patch16

  # Pretrain model kwargs
  pretrain_kwargs:
    img_size: 224
    in_chans: 3
    decoder_embed_dim: 512
    decoder_depth: 8
    decoder_num_heads: 16
    norm_pix_loss: false
    num_frames: 32
    t_patch_size: 4
    sep_pos_embed: true
    cls_embed: true

wandb:
  enabled: true
  project: echofm-probe-eval
  name: echofm-base-4class-probe